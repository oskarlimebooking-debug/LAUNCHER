<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:sharedUserId="android.uid.system"
    tools:replace="android:sharedUserId">

    <!-- Platform-signature permissions required by the system flavor.
         These are gated behind the platform keystore — without it, the flavor
         skips compilation and these permissions are never merged. -->

    <uses-permission android:name="android.permission.INJECT_EVENTS" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.SET_ACTIVITY_WATCHER"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.MANAGE_ACTIVITY_STACKS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.INTERNAL_SYSTEM_WINDOW"
        tools:ignore="ProtectedPermissions" />

    <!-- T2.7-followup — sharedUserId is a <manifest> attribute, not <application>;
         the prior placement inside <application> was silently dropped by the
         manifest merger (verified post-install: the system APK ran with UID 10065
         instead of 1000). Declaring it on <manifest> with tools:replace gives the
         merger the explicit override it needs against the main manifest, which
         has no sharedUserId. -->

</manifest>
