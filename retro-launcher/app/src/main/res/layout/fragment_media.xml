<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/media_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_album"
    android:padding="@dimen/tile_padding">

    <!-- Track title at the top, marqueeing if it overflows. Hidden when no
         session is active. The fragment only re-assigns the text when the
         string actually changes, so the marquee animation isn't restarted by
         the 250ms position ticker. -->
    <TextView
        android:id="@+id/title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:singleLine="true"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:scrollHorizontally="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:gravity="center_horizontal"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/track_title"
        android:fontFamily="sans-serif-medium"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Transport row: dead-center vertically when empty, slightly below
         center (bias 0.6) when a title sits above it. Buttons always present. -->
    <LinearLayout
        android:id="@+id/transport"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.5">

        <ImageButton
            android:id="@+id/prev"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_skip_prev"
            android:scaleType="fitCenter"
            android:contentDescription="@string/media_prev_cd" />

        <ImageButton
            android:id="@+id/play_pause"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="10dp"
            android:background="@drawable/bg_button_round"
            android:src="@drawable/ic_play"
            android:scaleType="fitCenter"
            android:contentDescription="@string/media_play_cd" />

        <ImageButton
            android:id="@+id/next"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_skip_next"
            android:scaleType="fitCenter"
            android:contentDescription="@string/media_next_cd" />
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
