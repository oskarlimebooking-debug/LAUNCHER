<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Core -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Location: speed, weather, trip -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Foreground service for trip recording -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Notification listener (for media controls) -->
    <uses-permission
        android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <!-- Wake to keep screen on while driving -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Boot receiver to auto-launch -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Battery whitelist (head unit can stay on under Doze) -->
    <uses-permission
        android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- App embedding (system flavor only — held back behind ENABLE_EMBEDDING in v0.1) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- osmdroid tile cache (offline MBTiles + cached tiles). API 23-28 only;
         on minSdk=23 / targetSdk=28 we still need this for sdcard tile dir. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Discover launchable apps for the drawer -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:resizeable="true"
        android:anyDensity="true" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.RetroLauncher"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        tools:targetApi="28">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:resumeWhilePausing="true"
            android:theme="@style/Theme.RetroLauncher.Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.wizard.WizardActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.RetroLauncher" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.RetroLauncher" />

        <activity
            android:name=".ui.settings.LicensesActivity"
            android:exported="false"
            android:label="@string/licenses_title"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.RetroLauncher" />

        <!-- Notification listener for media -->
        <service
            android:name=".service.MediaNotificationListener"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Foreground location/trip recorder. foregroundServiceType is intentionally
             unset: minSdk=23 / targetSdk=28 keeps us pre-API-29's type requirement. -->
        <service
            android:name=".service.LocationService"
            android:exported="false" />

        <!-- Boot receiver. Disabled by default so it cannot fire before the
             first-run wizard has set up permissions / OWM key. The wizard
             flips this on via BootReceiverGate.enable() on completion. -->
        <receiver
            android:name=".service.BootReceiver"
            android:enabled="false"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
