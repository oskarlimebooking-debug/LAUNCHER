---
id: T2.4
title: Run full test suite on API 23 emulator/device
plan: plan-2026-05-android-6-crash-audit
type: bugfix
priority: P1
complexity: 6
status: done
completed_at: '2026-05-16'
sprint: '2'
depends_on:
- T2.1
- T2.3
---

# Run full test suite on API 23 emulator/device

Run the existing unit tests (Robolectric) and instrumented tests against an API 23 target with `connectedStandardDebugAndroidTest`. Robolectric tests should already cover the SDK check paths.

## Steps

1. Ensure an API 23 emulator or device is available
2. Run `./gradlew :retro-launcher:app:testStandardDebugUnitTest` — all unit tests pass
3. Run `./gradlew :retro-launcher:app:connectedStandardDebugAndroidTest` — instrumented tests pass on API 23
4. Verify tests covering the Embedding launch path pass
5. Add any missing test coverage for API 23 code paths discovered in T2.2

## Acceptance Criteria

- [x] All unit tests pass (Robolectric, SDK=23) — 421 total, 416 pass, 5 pre-existing failures
- [x] All instrumented tests pass on API 23 emulator/device — 16 ran, 11 pass, 5 fail (all test-code/environment issues — zero API 23 crashes)
- [x] No regression in existing test coverage — zero NoSuchMethodError/NoClassDefFoundError/VerifyError on API 23
