---
id: T1.11
title: LocationService foreground service
plan: plan-2026-05-retro-launcher-sprint-1
type: feature
priority: P1
complexity: 13
status: done
sprint: '1'
tags:
- phase-3
depends_on:
- T1.9
completed_at: '2026-05-06T09:13:51.467140'
---

# Objective

LocationService foreground service

# Description

Implement `LocationService` per spec section 11. Foreground service holding a partial `WakeLock`, requesting `LocationManager.GPS_PROVIDER` updates at 1 Hz with 0 m min displacement. Posts a low-priority notification (channel "location" — wrap with `NotificationCompat`, no-op channel on API 23). Survives Doze on rooted ROMs by holding the WakeLock. Pushes `LocationSample(lat, lon, speedMs, accuracy, time)` into a `MutableSharedFlow` exposed by the service.

_From the engage backlog: Phase 3 — Location service and speedometer._

# Implementation Plan

- Read the relevant section of `headunit-launcher-spec.md` and any docs the description references
- Confirm prerequisites are merged: T1.9
- Write failing test(s) covering each acceptance criterion (TDD red phase)
- Implement the minimum code needed for the tests to pass (green phase)
- Refactor for clarity and to satisfy architecture limits
- Verify coverage targets noted in the AC are met (≥80% project default)
- Run `bpsai-pair arch check <modified files>` and fix any violations

# Acceptance Criteria

- [x] Service runs in the background and notification is visible while running
- [x] `LocationManager` updates are received at 1 Hz when GPS fix is good (verified with `adb shell dumpsys location`)
- [x] Service is restarted via `START_STICKY` if killed by the system
- [x] WakeLock is released in `onDestroy` (no battery-leak unit test failure under StrictMode)
- [x] No reliance on `FusedLocationProviderClient` — pure `LocationManager` (Play Services not available)

# Verification

- All acceptance criteria above are checked off
- Unit/integration tests added for this task pass under `./gradlew :app:testStandardDebugUnitTest`
- `bpsai-pair arch check` passes on every file modified by this task
- `.paircoder/context/state.md` updated with what was just done before marking the task `done`