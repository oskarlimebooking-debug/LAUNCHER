---
id: T1.14
title: SpeedFragment + ViewModel
plan: plan-2026-05-retro-launcher-sprint-1
type: feature
priority: P1
complexity: 5
status: done
sprint: '1'
tags:
- phase-3
depends_on:
- T1.13
completed_at: '2026-05-06T16:01:07.606403'
---

# Objective

SpeedFragment + ViewModel

# Description

Implement `SpeedFragment.kt` and `SpeedViewModel.kt`. ViewModel exposes `LiveData<SpeedUiState>` collected from `LocationRepository`'s filtered-speed flow. Fragment binds the value to `SpeedometerView`. Settings reload (unit/threshold change) reaches the view without fragment recreation.

_From the engage backlog: Phase 3 — Location service and speedometer._

# Implementation Plan

- Read the relevant section of `headunit-launcher-spec.md` and any docs the description references
- Confirm prerequisites are merged: T1.13
- Write failing test(s) covering each acceptance criterion (TDD red phase)
- Implement the minimum code needed for the tests to pass (green phase)
- Refactor for clarity and to satisfy architecture limits
- Run `bpsai-pair arch check <modified files>` and fix any violations

# Acceptance Criteria

- [x] Fragment displays the speedometer view in the right panel when the embed page is hidden (or as a tile in the left panel — final placement per HomeFragment)
- [x] Speed updates at the same rate as `LocationRepository` (1 Hz)
- [x] Switching `SettingsStore.speedUnit` triggers a `SpeedometerView` re-render with new units
- [x] Speedometer reads zero speed when no GPS fix is acquired
- [x] No memory leak when fragment is detached and reattached (verified with LeakCanary in debug builds)

# Verification

- All acceptance criteria above are checked off
- Unit/integration tests added for this task pass under `./gradlew :app:testStandardDebugUnitTest`
- `bpsai-pair arch check` passes on every file modified by this task
- `.paircoder/context/state.md` updated with what was just done before marking the task `done`