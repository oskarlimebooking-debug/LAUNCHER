---
id: T1.34
title: BootReceiver for auto-launch
plan: plan-sprint-1-engage
type: feature
priority: P2
complexity: 3
status: pending
sprint: '1'
depends_on:
- T1.33
---

# BootReceiver for auto-launch

Implement `BootReceiver.kt` to start `LocationService` and reschedule `WeatherWorker` on `BOOT_COMPLETED`. No UI activity start (the launcher is already the home app — system starts it).

# Acceptance Criteria

- [ ] Receiver triggers on `BOOT_COMPLETED` (verified with `adb shell am broadcast -a android.intent.action.BOOT_COMPLETED`)
- [ ] `LocationService` starts within 5 s of boot
- [ ] `WeatherWorker` is enqueued
- [ ] Receiver is `enabled` only when `firstRunDone = true` (avoid running before user setup)
- [ ] No reflection / hidden APIs used
