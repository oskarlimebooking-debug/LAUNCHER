---
id: T1.12
title: SpeedFilter with Kalman-style smoothing
plan: plan-2026-05-retro-launcher-sprint-1
type: feature
priority: P1
complexity: 8
status: done
sprint: '1'
tags:
- phase-3
depends_on:
- T1.11
completed_at: '2026-05-06T14:30:36.120290'
---

# Objective

SpeedFilter with Kalman-style smoothing

# Description

Implement `SpeedFilter.kt` per spec section 11.2. Takes raw `LocationSample` and produces a smoothed speed in m/s. Uses a 1D Kalman filter with process noise = 0.5 m/s² and measurement noise scaled from `accuracy` field. Falls back to a 5-sample moving average when GPS accuracy >20 m. Exposes filtered speed via `Flow<Float>`.

_From the engage backlog: Phase 3 — Location service and speedometer._

# Implementation Plan

- Read the relevant section of `headunit-launcher-spec.md` and any docs the description references
- Confirm prerequisites are merged: T1.11
- Write failing test(s) covering each acceptance criterion (TDD red phase)
- Implement the minimum code needed for the tests to pass (green phase)
- Refactor for clarity and to satisfy architecture limits
- Verify coverage targets noted in the AC are met (≥80% project default)
- Run `bpsai-pair arch check <modified files>` and fix any violations

# Acceptance Criteria

- [x] Filter converges to true speed within 5 samples when given clean GPS input (verified with synthetic 1 Hz test)
- [x] When measurement noise is high, filter weights process model more — verified with a unit test injecting accuracy=100 m
- [x] Outputs zero speed within 2 samples of `LocationSample.speedMs == 0`
- [x] Cold-start sample produces a non-NaN output (no division-by-zero on the first reading)
- [x] Unit test coverage ≥90% for the filter logic

# Verification

- All acceptance criteria above are checked off
- Unit/integration tests added for this task pass under `./gradlew :app:testStandardDebugUnitTest`
- `bpsai-pair arch check` passes on every file modified by this task
- `.paircoder/context/state.md` updated with what was just done before marking the task `done`