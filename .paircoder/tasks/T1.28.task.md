---
id: T1.28
title: AppListRepository PackageManager scanning
plan: plan-sprint-1-engage
type: feature
priority: P1
complexity: 8
status: in_progress
sprint: '1'
depends_on:
- T1.10
---

# AppListRepository PackageManager scanning

Implement `AppListRepository.kt` per spec section 13.1. Queries `PackageManager.queryIntentActivities(MAIN, LAUNCHER)` to list installed launchable apps. Caches the list (rebuilds on `PACKAGE_ADDED`/`PACKAGE_REMOVED` broadcasts). Loads icons via Glide (using a custom `ApplicationInfo` model loader to keep cache hits). Sorts by user-defined order from `SettingsStore`.

# Acceptance Criteria

- [x] First scan completes in under 500 ms on a 1 GB device with 30 apps
- [x] Adding/removing an app updates the list within 1 s via broadcast receiver
- [x] Icons are loaded asynchronously and cached (Glide disk + memory)
- [x] Sort order persists across launches via `SettingsStore`
- [x] Excluded system apps (e.g., the launcher itself) are filtered out