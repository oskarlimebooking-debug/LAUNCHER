---
id: T1.10
title: 'Util extensions: ColorExt, FormatExt, ViewExt'
plan: plan-2026-05-retro-launcher-sprint-1
type: feature
priority: P1
complexity: 3
status: done
sprint: '1'
tags:
- phase-2
depends_on:
- T1.6
completed_at: '2026-05-06T08:47:46.573363'
---

# Objective

Util extensions: ColorExt, FormatExt, ViewExt

# Description

Per spec section 17 — create three small extension files. `ColorExt.kt` with palette-derived dominant-color helpers. `FormatExt.kt` with `formatDistance`, `formatDuration`, `formatSpeed` accepting unit prefs. `ViewExt.kt` with `View.fade()`, `View.gone()`, `View.visible()`. Each helper has a unit test.

_From the engage backlog: Phase 2 — App shell._

# Implementation Plan

- Read the relevant section of `headunit-launcher-spec.md` and any docs the description references
- Confirm prerequisites are merged: T1.6
- Write failing test(s) covering each acceptance criterion (TDD red phase)
- Implement the minimum code needed for the tests to pass (green phase)
- Refactor for clarity and to satisfy architecture limits
- Verify coverage targets noted in the AC are met (≥80% project default)
- Run `bpsai-pair arch check <modified files>` and fix any violations

# Acceptance Criteria

- [x] `formatDistance(123.456, Unit.METRIC)` returns `"123 m"` and `(1234.5, METRIC)` returns `"1.2 km"`
- [x] `formatDuration(3725)` returns `"01:02:05"`
- [x] `formatSpeed(20.5, IMPERIAL)` returns `"45.9 mph"`
- [x] Each extension is in its own file and under 50 lines (architecture rule)
- [x] Unit tests cover each public function with at least 3 cases including a zero/edge case

# Verification

- All acceptance criteria above are checked off
- Unit/integration tests added for this task pass under `./gradlew :app:testStandardDebugUnitTest`
- `bpsai-pair arch check` passes on every file modified by this task
- `.paircoder/context/state.md` updated with what was just done before marking the task `done`