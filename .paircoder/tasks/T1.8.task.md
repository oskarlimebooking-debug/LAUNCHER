---
id: T1.8
title: HomeFragment with ViewPager2 right panel
plan: plan-2026-05-retro-launcher-sprint-1
type: feature
priority: P0
complexity: 8
status: done
sprint: '1'
tags:
- phase-2
depends_on:
- T1.7
completed_at: '2026-05-06T08:04:17.926277'
---

# Objective

HomeFragment with ViewPager2 right panel

# Description

Implement `HomeFragment` per spec section 17.5. Two-column layout: left panel (Media + Weather stacked, 40% width) and right panel (`ViewPager2` swiping between Embed/AppGrid/Trips, 60% width). Panel split ratio is read from `SettingsStore`. Implement `RightPanelAdapter` returning the three fragments by position.

_From the engage backlog: Phase 2 — App shell._

# Implementation Plan

- Read the relevant section of `headunit-launcher-spec.md` and any docs the description references
- Confirm prerequisites are merged: T1.7
- Write failing test(s) covering each acceptance criterion (TDD red phase)
- Implement the minimum code needed for the tests to pass (green phase)
- Refactor for clarity and to satisfy architecture limits
- Run `bpsai-pair arch check <modified files>` and fix any violations

# Acceptance Criteria

- [x] `fragment_home.xml` uses `Guideline` for the panel split, position bound to `app:layout_constraintGuide_percent` from settings
- [x] Swiping the right panel cycles between the three fragments smoothly (no jank on a Cortex-A7)
- [x] `RightPanelAdapter` extends `FragmentStateAdapter` and returns `EmbedFragment`/`AppGridFragment`/`TripsFragment`
- [x] Panel split ratio change triggers a layout pass without recreating the activity (use `ConstraintLayout.LayoutParams` updates)
- [x] Default split is 0.4/0.6 as per spec section 8.1

# Verification

- All acceptance criteria above are checked off
- Unit/integration tests added for this task pass under `./gradlew :app:testStandardDebugUnitTest`
- `bpsai-pair arch check` passes on every file modified by this task
- `.paircoder/context/state.md` updated with what was just done before marking the task `done`