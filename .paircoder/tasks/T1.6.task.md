---
id: T1.6
title: App.kt service locator and Application class
plan: plan-2026-05-retro-launcher-sprint-1
type: feature
priority: P0
complexity: 5
status: done
sprint: '1'
tags:
- phase-2
depends_on:
- T1.5
completed_at: '2026-05-06T07:33:34.475468'
---

# Objective

App.kt service locator and Application class

# Description

Implement `App.kt` per spec section 17.1. Application subclass with manual DI via a `service` object. Initialize Timber in debug, expose lazily-created repositories (`MediaRepository`, `WeatherRepository`, `LocationRepository`, `TripRepository`, `AppListRepository`, `SettingsStore`). Use lazy initialization so we don't pay startup cost for things we may not touch this session.

_From the engage backlog: Phase 2 — App shell._

# Implementation Plan

- Read the relevant section of `headunit-launcher-spec.md` and any docs the description references
- Confirm prerequisites are merged: T1.5
- Write failing test(s) covering each acceptance criterion (TDD red phase)
- Implement the minimum code needed for the tests to pass (green phase)
- Refactor for clarity and to satisfy architecture limits
- Run `bpsai-pair arch check <modified files>` and fix any violations

# Acceptance Criteria

- [x] `App` is registered in `AndroidManifest.xml` via `android:name=".App"`
- [x] `App.service` is accessible from any Activity/Fragment via `(application as App).service`
- [x] Timber is planted with `Timber.DebugTree()` only in `BuildConfig.DEBUG`
- [x] Each repo is `by lazy {}` and constructed with the application context
- [x] No service-locator dependency on Activity context (would leak)

# Verification

- All acceptance criteria above are checked off
- Unit/integration tests added for this task pass under `./gradlew :app:testStandardDebugUnitTest`
- `bpsai-pair arch check` passes on every file modified by this task
- `.paircoder/context/state.md` updated with what was just done before marking the task `done`