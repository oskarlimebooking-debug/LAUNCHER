---
id: T1.22
title: WeatherFragment + ViewModel + icon mapping
plan: plan-sprint-1-engage
type: feature
priority: P1
complexity: 5
status: in_progress
sprint: '1'
depends_on:
- T1.21
---

# WeatherFragment + ViewModel + icon mapping

Implement `WeatherFragment.kt` + `WeatherViewModel.kt` + the icon-mapping utility per spec section 10.6. Layout shows current temp, feels-like, conditions text, wind, and a weather icon. Map OWM icon codes (`01d`, `01n`, …) to local vector drawables. Refreshes from `WeatherRepository.snapshot`.

# Acceptance Criteria

- [x] All 18 OWM icon codes are mapped to local SVG drawables (no remote icon fetches)
- [x] Fragment displays placeholder ("—") when snapshot is null
- [x] Temp displays in C° or F° based on `SettingsStore.tempUnit`
- [x] Wind speed displays in m/s, km/h, or mph based on `SettingsStore.speedUnit`
- [x] Tile is exactly half the height of the left panel (matches spec section 8.1)