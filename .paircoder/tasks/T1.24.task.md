---
id: T1.24
title: TripRecorder state machine
plan: plan-sprint-1-engage
type: feature
priority: P1
complexity: 13
status: done
sprint: '1'
depends_on:
- T1.23
completed_at: '2026-05-09T16:32:54.244164'
---

# TripRecorder state machine

Implement `TripRecorder.kt` per spec section 12.2. State machine: `Idle → Detecting → Recording → Stopping → Idle`. Transitions: `Idle→Detecting` when a `LocationSample` exceeds `tripStartSpeedKmh` (default 10), `Detecting→Recording` when sustained for 10 s, `Recording→Stopping` when speed <2 km/h sustained for 60 s, `Stopping→Idle` after a final `TripEntity` insert. Aggregates points into 5-second buckets to reduce DB writes.

# Acceptance Criteria

- [x] State machine never enters an invalid transition (verified with property-based tests)
- [x] A 10-minute synthetic GPS trace produces exactly one `TripEntity` with the correct distance (within ±5%)
- [x] Stopping at a traffic light for 30 s does NOT split the trip (well under the 60 s threshold)
- [x] Bucket aggregation reduces DB inserts by ≥80% vs. raw 1 Hz writes
- [x] Recovery: if the service is killed mid-trip, the partial trip is recovered on next start (state is persisted to `SharedPreferences`)