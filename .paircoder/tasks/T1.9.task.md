---
id: T1.9
title: StatusBarFragment (clock, speed, trip stats, drawer button)
plan: plan-2026-05-retro-launcher-sprint-1
type: feature
priority: P0
complexity: 5
status: done
sprint: '1'
tags:
- phase-2
depends_on:
- T1.8
completed_at: '2026-05-06T08:39:43.420654'
---

# Objective

StatusBarFragment (clock, speed, trip stats, drawer button)

# Description

Implement `StatusBarFragment` per spec section 16. Bottom strip showing: clock (left), current speed (center), trip distance/duration (right), drawer button (far right). Updates from `LocationRepository` (speed) and `TripRepository` (active-trip stats) via `LiveData`. Drawer button opens settings.

_From the engage backlog: Phase 2 — App shell._

# Implementation Plan

- Read the relevant section of `headunit-launcher-spec.md` and any docs the description references
- Confirm prerequisites are merged: T1.8
- Write failing test(s) covering each acceptance criterion (TDD red phase)
- Implement the minimum code needed for the tests to pass (green phase)
- Refactor for clarity and to satisfy architecture limits
- Run `bpsai-pair arch check <modified files>` and fix any violations

# Acceptance Criteria

- [x] Clock updates every second using a `Handler`-based ticker (no `BroadcastReceiver` overhead — saves CPU)
- [x] Speed display switches between km/h and mph based on `SettingsStore.speedUnit`
- [x] Trip stats appear only when a trip is active (visibility GONE otherwise)
- [x] Drawer button starts `SettingsActivity` via an intent
- [x] Layout fits within `@dimen/status_bar_height` (48dp at 1024dp width)

# Verification

- All acceptance criteria above are checked off
- Unit/integration tests added for this task pass under `./gradlew :app:testStandardDebugUnitTest`
- `bpsai-pair arch check` passes on every file modified by this task
- `.paircoder/context/state.md` updated with what was just done before marking the task `done`