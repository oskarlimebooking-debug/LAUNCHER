---
id: T1.2
title: Author app/build.gradle.kts with flavors
plan: plan-2026-05-retro-launcher-sprint-1
type: feature
priority: P0
complexity: 5
status: done
sprint: '1'
tags:
- phase-1
depends_on:
- T1.1
completed_at: '2026-05-05T21:58:19.413527'
---

# Objective

Author app/build.gradle.kts with flavors

# Description

Create the `app/build.gradle.kts` per spec section 6. Two product flavors: `standard` (any user, no embedding) and `system` (platform-signed, embedding enabled). `compileSdk = 34`, `minSdk = 23`, `targetSdk = 28`. Enable `viewBinding`, `kapt` for Room and Moshi codegen and Glide annotation processor. Add a packaging exclude for `META-INF/{AL2.0,LGPL2.1}`.

_From the engage backlog: Phase 1 — Project scaffolding._

# Implementation Plan

- Read the relevant section of `headunit-launcher-spec.md` and any docs the description references
- Confirm prerequisites are merged: T1.1
- Write failing test(s) covering each acceptance criterion (TDD red phase)
- Implement the minimum code needed for the tests to pass (green phase)
- Refactor for clarity and to satisfy architecture limits
- Run `bpsai-pair arch check <modified files>` and fix any violations

# Acceptance Criteria

- [x] `./gradlew :app:assembleStandardDebug` produces an APK (even if empty)
- [x] Two flavors `standard` and `system` are visible in `./gradlew :app:tasks`
- [x] `system` flavor has a `signingConfig` placeholder pointing to a `platform.keystore` (file may not exist yet — placeholder is enough)
- [x] ViewBinding is enabled and `kapt` is configured for Room, Moshi-codegen, and Glide-compiler
- [x] Lint baseline file is created at `app/lint-baseline.xml` to suppress pre-existing AndroidX warnings

# Verification

- All acceptance criteria above are checked off
- Unit/integration tests added for this task pass under `./gradlew :app:testStandardDebugUnitTest`
- `bpsai-pair arch check` passes on every file modified by this task
- `.paircoder/context/state.md` updated with what was just done before marking the task `done`