---
id: T1.23
title: Room schema (TripEntity, TripPoint, TripDao, AppDb)
plan: plan-sprint-1-engage
type: feature
priority: P1
complexity: 8
status: pending
sprint: '1'
depends_on:
- T1.10
---

# Room schema (TripEntity, TripPoint, TripDao, AppDb)

Implement the Room schema per spec section 12.3. `TripEntity(id, startTime, endTime, distanceM, durationS, maxSpeedMs, avgSpeedMs, gpxPath?)`. `TripPoint(tripId, lat, lon, speedMs, time)` linked by foreign key. `TripDao` with insertTrip, insertPoints (bulk), getRecentTrips(limit), getPointsForTrip(tripId), deleteTrip(tripId). `AppDb` extends `RoomDatabase` and exposes the DAO.

# Acceptance Criteria

- [ ] Database migrates from version 0→1 cleanly (no migration needed yet, but `Room.fallbackToDestructiveMigration` is NOT used — explicit `Migration` objects only)
- [ ] Foreign-key cascade deletes points when a trip is deleted
- [ ] DAO methods are `suspend` or return `Flow` (no blocking on the main thread)
- [ ] Room compiles without warnings under `kapt`
- [ ] Unit tests using Room's in-memory builder cover insert/query/delete for ≥4 trips with 100 points each
