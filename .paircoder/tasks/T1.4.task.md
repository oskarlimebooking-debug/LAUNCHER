---
id: T1.4
title: 'Resource bundles: themes, colors, strings, dimens'
plan: plan-2026-05-retro-launcher-sprint-1
type: feature
priority: P0
complexity: 5
status: done
sprint: '1'
tags:
- phase-1
depends_on:
- T1.3
completed_at: '2026-05-05T22:12:26.659423'
---

# Objective

Resource bundles: themes, colors, strings, dimens

# Description

Author the resource bundles per spec section 8. `values/themes.xml` with `Theme.RetroLauncher` (Material3 dark), `values/colors.xml` with neutral surfaces and accent, `values/strings.xml` for all user-facing copy, `values-w1024dp/dimens.xml` for the landscape grid (column gutter, panel radius, status bar height), plus `values-night/colors.xml` for night theme. Include shape and typography appendix per spec section 8.4.

_From the engage backlog: Phase 1 — Project scaffolding._

# Implementation Plan

- Read the relevant section of `headunit-launcher-spec.md` and any docs the description references
- Confirm prerequisites are merged: T1.3
- Write failing test(s) covering each acceptance criterion (TDD red phase)
- Implement the minimum code needed for the tests to pass (green phase)
- Refactor for clarity and to satisfy architecture limits
- Run `bpsai-pair arch check <modified files>` and fix any violations

# Acceptance Criteria

- [x] `Theme.RetroLauncher` extends `Theme.Material3.DayNight.NoActionBar` and sets `windowBackground`, `colorPrimary`, `colorSurface`
- [x] `dimens.xml` defines `gutter`, `corner_lg`, `corner_md`, `panel_left_min_width`, `status_bar_height`, `tile_corner`
- [x] All user-facing strings in spec section 8 are in `strings.xml` with English values
- [x] Day and night theme variants build cleanly under `assembleStandardDebug`
- [x] No hard-coded colors or strings in any layout file (verified by `lint --check HardcodedText,HardcodedColor`)

# Verification

- All acceptance criteria above are checked off
- Unit/integration tests added for this task pass under `./gradlew :app:testStandardDebugUnitTest`
- `bpsai-pair arch check` passes on every file modified by this task
- `.paircoder/context/state.md` updated with what was just done before marking the task `done`