---
id: T1.7
title: MainActivity and activity_main.xml
plan: plan-2026-05-retro-launcher-sprint-1
type: feature
priority: P0
complexity: 5
status: done
sprint: '1'
tags:
- phase-2
depends_on:
- T1.6
completed_at: '2026-05-06T07:41:02.065114'
---

# Objective

MainActivity and activity_main.xml

# Description

Implement `MainActivity` per spec section 17.2. Single-activity launcher. Hosts `HomeFragment` in a top container and `StatusBarFragment` in a bottom strip. Lock to landscape, `WindowCompat.setDecorFitsSystemWindows(window, false)` for edge-to-edge. Handle `onBackPressed` to no-op (a launcher should not exit on back).

_From the engage backlog: Phase 2 — App shell._

# Implementation Plan

- Read the relevant section of `headunit-launcher-spec.md` and any docs the description references
- Confirm prerequisites are merged: T1.6
- Write failing test(s) covering each acceptance criterion (TDD red phase)
- Implement the minimum code needed for the tests to pass (green phase)
- Refactor for clarity and to satisfy architecture limits
- Run `bpsai-pair arch check <modified files>` and fix any violations

# Acceptance Criteria

- [x] `activity_main.xml` is a vertical `ConstraintLayout` with `home_container` (0dp,1.0 weight) above `status_bar_container` (height = `@dimen/status_bar_height`)
- [x] Activity launches and shows the empty fragment containers without crashing
- [x] Back press does not finish the activity (`onBackPressed` is a no-op)
- [x] Edge-to-edge insets are consumed; no system bar overlap on the 1024×600 viewport
- [x] Activity is set as default launcher via `intent-filter` (verified manually by long-pressing home icon and choosing it)

# Verification

- All acceptance criteria above are checked off
- Unit/integration tests added for this task pass under `./gradlew :app:testStandardDebugUnitTest`
- `bpsai-pair arch check` passes on every file modified by this task
- `.paircoder/context/state.md` updated with what was just done before marking the task `done`