# Security Memory

> This file is automatically loaded into the Security agent's system prompt (first 200 lines).
> Record security boundaries, blocked operations, and vulnerability patterns specific to this project.

## Protected Paths
- `.claude/agents/`, `.claude/commands/`, `.claude/skills/`
- `.env`, `.env.local`, `.env.production`
- `credentials.json`, `secrets.yaml`

## Dependency Security
<!-- Record dependency audit findings -->

## Areas Requiring Attention
<!-- Record areas with ongoing security concerns -->
